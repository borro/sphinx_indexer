parameters:
  git_dir: .
  bin_dir: ./vendor/bin
  tasks:
    phpcs:
      standard: "PSR2"
      show_warnings: false
    blacklist:
      keywords:
        - "die("
        - "var_dump("
        - "print_r("
        - "exit;"
